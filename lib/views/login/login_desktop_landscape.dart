import 'package:flutter/material.dart';

class LoginViewDesktopLandscape extends StatelessWidget {
  const LoginViewDesktopLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
