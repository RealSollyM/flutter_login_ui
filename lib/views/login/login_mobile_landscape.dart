import 'package:flutter/material.dart';

class LoginViewMobileLandscape extends StatelessWidget {
  const LoginViewMobileLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
