import 'package:flutter/material.dart';

class LoginViewTabletPortrait extends StatelessWidget {
  const LoginViewTabletPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
