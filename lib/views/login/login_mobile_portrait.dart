import 'package:flutter/material.dart';

class LoginViewMobilePortrait extends StatelessWidget {
  const LoginViewMobilePortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
