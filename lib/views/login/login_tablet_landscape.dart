import 'package:flutter/material.dart';

class LoginViewTabletLandscape extends StatelessWidget {
  const LoginViewTabletLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
