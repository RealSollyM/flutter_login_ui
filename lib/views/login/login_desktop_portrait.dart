import 'package:flutter/material.dart';

class LoginViewDesktopPortrait extends StatelessWidget {
  const LoginViewDesktopPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
