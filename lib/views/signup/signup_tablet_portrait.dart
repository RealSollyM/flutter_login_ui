import 'package:flutter/material.dart';

class SignupViewTabletPortrait extends StatelessWidget {
  const SignupViewTabletPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
