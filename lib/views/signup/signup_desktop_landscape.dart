import 'package:flutter/material.dart';

class SignupViewDesktopLandscape extends StatelessWidget {
  const SignupViewDesktopLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
