import 'package:flutter/material.dart';

class SignupViewDesktopPortrait extends StatelessWidget {
  const SignupViewDesktopPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
