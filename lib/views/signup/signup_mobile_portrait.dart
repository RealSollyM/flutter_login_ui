import 'package:flutter/material.dart';

class SignupViewMobilePortrait extends StatelessWidget {
  const SignupViewMobilePortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
