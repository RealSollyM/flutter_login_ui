import 'package:flutter/material.dart';

class SignupViewMobileLandscape extends StatelessWidget {
  const SignupViewMobileLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
