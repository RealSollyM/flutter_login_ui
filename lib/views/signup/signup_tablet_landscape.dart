import 'package:flutter/material.dart';

class SignupViewTabletLandscape extends StatelessWidget {
  const SignupViewTabletLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
